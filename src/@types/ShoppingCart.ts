export default interface ShoppingCartProps {
  id: number;
  name: string;
  // retailPrice: string;
  // retailPromotionPrice: string;
  image: string;
  // offer?: boolean;
  // promotion?: boolean;
  url: string;
  quantity: number;
}
